<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application>
    <receiver
      android:name="expo.modules.echowidget.EchoWidgetProvider"
      android:exported="true">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
        <action android:name="expo.modules.echowidget.REFRESH" />
      </intent-filter>
      <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/echo_widget_info" />
    </receiver>

    <service
      android:name="expo.modules.echowidget.EchoWidgetService"
      android:permission="android.permission.BIND_REMOTEVIEWS"
      android:exported="false" />

    <!-- invisible trampoline so widget taps can start the FG NarrationService
         (a broadcast-context FGS start is denied on Android 12+) -->
    <activity
      android:name="expo.modules.echowidget.EchoWidgetActivity"
      android:exported="false"
      android:theme="@android:style/Theme.Translucent.NoTitleBar"
      android:excludeFromRecents="true"
      android:noHistory="true"
      android:taskAffinity=""
      android:launchMode="singleTask" />
  </application>
</manifest>
